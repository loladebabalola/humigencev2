# Humigence CLI Requirements
# Pinned versions for stability

# Core ML libraries
transformers>=4.41.0,<5.0.0
torch>=2.1.0
torchvision
torchaudio
peft>=0.7.0
datasets>=2.14.0
accelerate>=0.24.0
bitsandbytes>=0.41.0

# Unsloth for dual-GPU LoRA training
unsloth @ git+https://github.com/unslothai/unsloth.git

# CLI and UI
rich>=13.0.0
inquirer>=3.1.0
typer>=0.9.0

# Data handling
numpy>=1.24.0
pandas>=2.0.0
tqdm>=4.65.0
psutil>=5.9.0

# System utilities
pathlib2>=2.3.7
click>=8.0.0

# Optional: for advanced features
# wandb>=0.15.0
# tensorboard>=2.13.0
