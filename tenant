#!/usr/bin/env python3
"""
Tenant CLI - Multi-Tenant Inference Command Line Interface
Usage: tenant <command> [options]

Commands:
  add <alias> [options]     - Add a new tenant
  remove <alias>            - Remove a tenant
  update <alias> [options]  - Update tenant settings
  list                      - List all tenants
  status                    - Show tenant statistics
  infer <tenant> <prompt>   - Run inference as a tenant
"""

import sys
import os
from pathlib import Path

# Add the humigence directory to Python path
humigence_dir = Path(__file__).parent
sys.path.insert(0, str(humigence_dir))

# Import and run the tenant manager CLI
from inference.tenant_manager import app

if __name__ == "__main__":
    app()
